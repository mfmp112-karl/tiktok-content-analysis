"""Outside demand signal — what the niche is talking about right now.

Everything here is optional and everything degrades. A run with no research
still produces a full report and a full calendar; it just says so. What is not
allowed is a partial pull that reads like a complete one, so every source
reports whether it was actually reached.
"""
